rootProject.name = 'aether-ii'

include 'orbis-api'

project(':orbis-api').projectDir = file("lib/OrbisAPI")

def phosphorSubmodule = file("lib/Phosphor")

if (phosphorSubmodule.exists()) {
    include 'phosphor'

    project(":phosphor").projectDir = phosphorSubmodule
}
package com.gildedgames.aether.client.ui.minecraft.util.file_system;

public interface IFileBrowserListener
{
	/*void onOpened(GuiFileBrowserButton fileButton, IOFile<?, ?> openedFile);

	/**
	 * This is only used in the file browser list
	 * @param files
	 */
	/*void onAdded(GuiFileBrowserButton file);

	void onDelete(List<GuiFileBrowserButton> files);

	void onSelect(List<GuiFileBrowserButton> buttons);

	void onUnselect(List<GuiFileBrowserButton> newParam);

	void onOpenMultiple(List<GuiFileBrowserButton> buttons);*/
}
